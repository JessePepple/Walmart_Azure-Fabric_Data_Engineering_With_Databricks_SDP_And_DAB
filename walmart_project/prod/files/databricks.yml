# This is a Declarative Automation Bundle definition for walmart_project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: walmart_project
  uuid: 0e838fc6-3cdb-4d7e-8600-7c394ada6f99

include:
  - resources/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
    default: walmart_catalog
  schema:
    description: The schema to use
    default: default

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405616563791296.16.azuredatabricks.net/
    variables:
      catalog: walmart_catalog
      schema: ${workspace.current_user.short_name}
      
    presets:
      name_prefix: ${workspace.current_user.short_name}
      source_linked_deployment: false
  prod:
    mode: production
    workspace:
      host: https://adb-7405616563791296.16.azuredatabricks.net/
      # We explicitly deploy to /Workspace/Users/jessepepple36@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/jessepepple36@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: walmart_catalog
      schema: prod
    permissions:
      - user_name: jessepepple36@gmail.com
        level: CAN_MANAGE
